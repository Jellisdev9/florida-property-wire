# See https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files for more about ignoring files.

# compiled output
dist
tmp
out-tsc
*.tsbuildinfo
.expo
.expo-shared

# dependencies
node_modules

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# misc
/.sass-cache
/connect.lock
/coverage
/libpeerconnection.log
npm-debug.log
yarn-error.log
testem.log
/typings

# System Files
.DS_Store
Thumbs.db

.cursor/rules/nx-rules.mdc
.github/instructions/nx.instructions.md

# Replit
.cache/
.local/

# Python / Django
__pycache__/
*.pyc
.venv/
backend/.env
backend/tailwindcss
backend/staticfiles/

# Docker Compose var-substitution file (DB_NAME/USER/PASSWORD, SITE_ADDRESS)
/.env

# Local dev convenience only — publishes db's Postgres port to the host so
# host-based `manage.py test`/`runserver` can reach it without a native
# Postgres install. Must never be committed: docker-compose.yml's deploy
# job runs plain `docker compose up -d`, which auto-loads this file if
# present — committing it would expose Postgres publicly on the VPS.
/docker-compose.override.yml

# Local-only planning/scratch docs — not durable project docs like
# BUILD_PLAN.md, still being figured out, not meant to be committed.
# planning/ holds the active spec + structural docs; planning-archive/ is
# where docs land once the work they describe is done or superseded.
planning/
planning-archive/
